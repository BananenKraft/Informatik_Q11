Aufgabe 1: Beschreiben sie Vor- als auch Nachteile, die sich bei der Implementierung einer Liste mit Hilfe von Arrays ergeben.
Nachteile:
 - Muss fülle der Liste selbst überprüfen
 - Muss manuell vergrößern
Vorteile:
 - Leicht vorstellbar
 - Schneller Zugriff auf Positionen
 - Festgelegte Anzahl

Aufgabe 2: Schreiben sie eine Metohde int countOfHuman(Human human){}

public int countOfHUman(Human human){
  int matches = 0;
  for (i=0;i<count;i++){
    if (human.getname() == queue[i].getname() && human.getage() == queue[i].getage()){
      matches++;
    }
  }
  return matches;
}